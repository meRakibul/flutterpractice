import 'package:flutter/cupertino.dart';

class Homefragement extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Center(
        child: Text("This is Home Friend"),
      ),
    );
  }

}