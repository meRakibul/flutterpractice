import 'package:flutter/cupertino.dart';

class Settingsfragement extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Center(
        child: Text("This is Search Fragement"),
      ),
    );
  }

}